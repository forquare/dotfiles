# Set default Xft(3) font 
#fontname "Ubuntu:pixelsize=14:bold" 
 
# Turn on sticky-group mode 
sticky yes
 
# Any entry here is shown in the application menu 
command term 	  "uxterm"
command lock 	  "slock"
command Browser	  firefox
command Email	  thunderbird
command Files	  lumina-fm
 
# Autogroup definitions 
autogroup 1 "xclock,XClock"
 
# Ignore programs by that name by not drawing borders around them. 
ignore xclock
 
# Key bindings 
bind-key CM-r		window-menu-label 
bind-key C4-equal	window-vmaximize 
bind-key C4S-equal	window-hmaximize 
bind-key M-1		group-only-1 
bind-key M-2		group-only-2 
bind-key M-3		group-only-3 
bind-key M-4		group-only-4
bind-key M-5		group-only-5
bind-key MS-1		window-movetogroup-1 
bind-key MS-2		window-movetogroup-2 
bind-key MS-3		window-movetogroup-3 
bind-key MS-4		window-movetogroup-4
bind-key MS-5		window-movetogroup-5
unbind-key 4-o 
unbind-key CM-equal 
unbind-key CMS-equal 

#These normally move from one group to another - this unbinds them
unbind-key M-Right
unbind-key M-Left
 
# Mouse bindings 
bind-mouse M-2		window-lower 
bind-mouse M-3		window-resize

#Color options
color menubg gray
color font black
color activeborder blue
color inactiveborder red
color selfont white

#window options
borderwidth 2
